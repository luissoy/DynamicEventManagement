version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: notification-app
    ports:
      - "10002:8080"
    environment:
      API_BASE_URL: http://database-api:8080
    networks:
      - internal

networks:
  internal:
    driver: bridge